# Map Filter Reduce Playground

Open `MapFilterReduce.playground` in Xcode, then run the playground.

This playground demonstrates three common Swift collection methods:

- `map`, which transforms each item into a new value.
- `filter`, which keeps only items that match a condition.
- `reduce`, which combines many values into one result.

The final example chains all three methods together.
