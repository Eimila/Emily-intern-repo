# Decoding JSON Playground

Open `DecodingJSON.playground` in Xcode, then run the playground.

This playground demonstrates how to decode JSON into Swift types using:

- `Codable` structs.
- `JSONDecoder`.
- custom `CodingKeys` for snake_case JSON fields.
- `do`, `try`, and `catch` error handling.
